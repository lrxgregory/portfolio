---
import './card-portfolio.css';
const {
	title,
	descripcion,
	imgSrc,
	skills,
	repoURL,
	demoURL,
	averageBrightness,
	lang, // Extraire la langue passée comme prop
} = Astro.props;
---

<div class="card__portfolio">
	<img
		src={imgSrc}
		alt={title[lang]}
		crossorigin="anonymous"
		height={130}
		width={332}
		loading="lazy"
		average-brightness={averageBrightness}
	/>

	<div class="descripcion__container">
		<h2 class="card__title">{title[lang]}</h2>
		<!-- Titre traduit -->

		<div class="descripcion">
			<p>{descripcion[lang]}</p>
			<!-- Description traduite -->

			<div class="skills">
				{
					skills.map((skill: string) => (
						<iconify-icon class="iconify" icon={skill} />
					))
				}
			</div>

			<div class="buttons">
				<a href={repoURL}>
					<span>
						Github
						{(<iconify-icon icon="quill:link-out" />)}
					</span>
				</a>

				<a href={demoURL}>
					<span>
						Demo
						{(<iconify-icon icon="quill:link-out" />)}
					</span>
				</a>
			</div>
		</div>
	</div>
</div>
